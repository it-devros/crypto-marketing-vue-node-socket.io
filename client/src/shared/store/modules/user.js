
const initialState = {
};

const getters = {
};

const actions = {
  isAuthed() {
    return Promise.resolve(true);
  },
};

const mutations = {
};

export default {
  namespaced: true,
  state: initialState,
  getters,
  actions,
  mutations,
};
